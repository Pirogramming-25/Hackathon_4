# 협업 가이드 (Contributing)

스마트 한걸음 프로젝트의 협업 규칙입니다. 작업 전에 한 번씩 읽어주세요. 🙌

---

## 🌿 브랜치 전략

| 브랜치    | 역할      | 설명                                                           |
| --------- | --------- | -------------------------------------------------------------- |
| `main`    | 배포용    | 항상 정상 동작하는 안정 버전만. 직접 push 금지, PR로만 병합    |
| `develop` | 개발 통합 | 모든 기능이 모이는 브랜치. 여기서 각자 기능 브랜치를 따서 작업 |

### 작업 흐름

```bash
# 1. 최신 develop 받기
git switch develop
git pull

# 2. 기능 브랜치 새로 파기 (develop에서 분기)
git switch -c feature/기능이름

# 3. 작업 후 커밋
git add .
git commit -m "feat: 기능 설명"

# 4. 원격에 올리기
git push origin feature/기능이름

# 5. GitHub에서 develop으로 PR 생성
```

### 브랜치 이름 규칙

- `feature/기능이름` — 새 기능 (예: `feature/course-list`, `feature/progress-api`)
- `fix/버그이름` — 버그 수정 (예: `fix/csrf-error`)

> 한 브랜치 = 한 가지 작업. 여러 기능을 한 브랜치에 섞지 않습니다.

---

## 📌 이슈 관리

작업을 시작하기 전에 **이슈부터 만듭니다.** 무엇을, 왜 하는지 먼저 정리하기 위함입니다.

- 이슈 템플릿(기능 / 버그 / 디자인)을 사용합니다.
- 제목은 `[FEAT] 코스 목록 화면 구현`처럼 유형을 앞에 붙입니다.
- 담당자(Assignee)와 라벨(Label)을 지정합니다.
- 브랜치와 PR은 이슈 번호와 연결합니다. (PR 본문에 `close #12`)

---

## 🔀 PR과 코드 리뷰

### PR 규칙

- 작업이 끝나면 `develop`으로 PR을 올립니다. (`main`으로 직접 X)
- PR 템플릿을 채웁니다. (작업 내용 / 스크린샷 / 체크리스트)
- PR 제목도 커밋 규칙을 따릅니다. (예: `feat: 코스 목록 화면 구현`)
- 하나의 PR은 되도록 작게 — 리뷰하기 쉽게 유지합니다.

### 리뷰 규칙

- **최소 1명 이상**의 승인(Approve) 후 병합합니다.
- 리뷰어는 24시간 내 확인을 목표로 합니다.
- 리뷰는 사람이 아니라 코드에 대한 것입니다. 서로 존중하며 남깁니다.
- 병합은 **Squash and merge**를 기본으로 합니다. (커밋 히스토리를 깔끔하게)
- 병합 후 기능 브랜치는 삭제합니다.

---

## ⚔️ 충돌(Conflict) 해결

여러 명이 같은 파일을 고치면 충돌이 날 수 있습니다. 당황하지 말고 아래 순서로 해결합니다.

```bash
# 1. 내 기능 브랜치에서 최신 develop을 당겨옴
git switch feature/내브랜치
git pull origin develop
```

충돌이 나면 파일에 아래 표시가 생깁니다.

```
<<<<<<< HEAD
내가 작업한 내용
=======
develop의 내용
>>>>>>> develop
```

- `<<<`, `===`, `>>>` 표시를 지우고 **어떤 코드를 남길지 직접 정리**합니다.
- 정리 후 저장하고 다시 커밋합니다.

```bash
git add .
git commit -m "fix: develop 병합 충돌 해결"
git push
```

> 🚨 혼자 해결이 어려우면 함께 처리합니다.
> 충돌을 대충 덮으면 남의 코드가 사라질 수 있으니 신중히.

---

## 📝 커밋 메시지 규칙

`타입: 설명` 형식으로 작성합니다.

| 타입       | 용도                    | 예시                                |
| ---------- | ----------------------- | ----------------------------------- |
| `feat`     | 새 기능                 | `feat: 챕터 진도 저장 API 추가`     |
| `fix`      | 버그 수정               | `fix: 프로그레스 바 갱신 오류 수정` |
| `docs`     | 문서                    | `docs: README 실행 방법 보강`       |
| `style`    | 포맷팅 (기능 변화 없음) | `style: 코드 정렬 및 공백 정리`     |
| `refactor` | 리팩토링                | `refactor: 진도 계산 로직 분리`     |
| `chore`    | 설정·잡일               | `chore: gunicorn 패키지 추가`       |

### 작성 팁

- 제목은 **무엇을 했는지** 명확하게, 50자 이내로.
- 한글/영어 상관없지만 팀 내에서 통일합니다.
- 하나의 커밋 = 하나의 논리적 변경.
